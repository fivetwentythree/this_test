# %% <- this would make the following code block act a REPL
import numpy as np

array_of_numbers = np.random.randint(1, 20, 100)
# print(array_of_numbers)
max_number = np.max(array_of_numbers)
print(max_number)
